<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="review.dao.ReviewMapper">

	<sql id="review-select">
		select  review_no, 
				title, 
				content, 
				picture_name, 
				hits, 
				recommend, 
				reg_date, 
				member_id
		from review
	</sql>
	
	<resultMap type="review" id="review-resultmap">
		<id column="review_no" property="reviewNo"/>
		<result column="title" property="title"/>
		<result column="content" property="content"/>
		<result column="picture_name" property="pictureName"/>
		<result column="hits" property="hits"/>
		<result column="recommend" property="recommend"/>
		<result column="reg_date" property="regDate"/>
		<result column="member_id" property="memberId"/>
	</resultMap>
	
	<!-- 등록 -->
	<insert id="insertReview" parameterType="review">
		<selectKey keyProperty="reviewNo" resultType="_int" order="BEFORE">
			select review_no_seq.nextval from dual
		</selectKey>
		insert into review (review_no, title, content, picture_name, hits, recommend, reg_date, member_id)
		values (#{reviewNo}, #{title}, #{content}, #{pictureName, jdbcType=VARCHAR}, #{hits}, #{recommend}, to_char(sysdate, 'yyyymmdd'), #{memberId})
	</insert>

	<!-- 수정 -->
	<update id="updateReview" parameterType="review">
		update  review
		set		title=#{title},
				content=#{content},
				<if test="pictureName!=null">
				,picture_name=#{pictureName}
				</if>
		where	member_id=#{memberId}
	</update>
	
	<!-- 삭제 -->

	<delete id="deleteReview" parameterType="int">
		delete from review where member_id=#{memberId}
	</delete>
	
	<!-- 리뷰 목록 페이징 -->
		<select id="selectAllReviewPaging" parameterType="map" resultMap="review-resultmap">
		select review_no, title, content, picture_name, hits, recommend, reg_date, member_id
		from (
				select ceil(rownum/#{contentsPerPage}) page, 
					   review_no, title, content, picture_name, hits, 
					   recommend, reg_date, member_id
				from (
						<include refid="review-select"/>
						order by reg_date desc
			)
		) 
		where	page = #{currentPage}
	</select>
	
	<!-- 게시물 수  -->
	<select id="selectTotalReviewCount" resultType="_int">
		select count(review_no) from review
	</select>

</mapper>
